apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: javan-todo-frontend
  namespace: storefront-devja
  annotations:
    "kubernetes.io/ingress.class": "alb"
    "alb.ingress.kubernetes.io/scheme": "internet-facing"
    "alb.ingress.kubernetes.io/target-type": "ip"
    "alb.ingress.kubernetes.io/listen-ports": "[{\"HTTP\": 80}]"
    "alb.ingress.kubernetes.io/healthcheck-path": "/"
    "alb.ingress.kubernetes.io/healthcheck-port": "traffic-port"
    "alb.ingress.kubernetes.io/healthcheck-interval-seconds": "15"
    "alb.ingress.kubernetes.io/healthy-threshold-count": "2"
    "alb.ingress.kubernetes.io/success-codes": "200-399"
    "alb.ingress.kubernetes.io/group.name": "storefront-devja-dev"
    "alb.ingress.kubernetes.io/group.order": "900"
  labels:
    app.kubernetes.io/name: javan-todo-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: javan-todo-frontend
                port:
                  number: 80
