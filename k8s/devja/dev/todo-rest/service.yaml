apiVersion: v1
kind: Service
metadata:
  name: javan-todo-rest
  namespace: storefront-devja
  labels:
    app.kubernetes.io/name: javan-todo-rest
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: javan-todo-rest
  ports:
    - name: http
      port: 80
      targetPort: 8080
